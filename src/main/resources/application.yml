server:
  port: 8028

logging:
  logback:
    rollingpolicy:
      max-file-size: 10MB
      max-history: 10
  file:
    name: logs/infomail.log
  pattern:
    dateformat: yyyy:MM:dd HH:mm:ss |

spring:

  config:
    import: sec.yml

  quartz:
    job-store-type: jdbc
    jdbc:
      initialize-schema: never
    properties:
      org.quartz.jobStore.driverDelegateClass: org.quartz.impl.jdbcjobstore.PostgreSQLDelegate
      org.quartz.jobStore.useProperties: true
      org.quartz.scheduler.instanceName: quartzScheduler
      org.quartz.scheduler.idleWaitTime: 5000

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        jdbc:
          lob:
            non_contextual_creation: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${infomail.email}
    password: ${infomail.password}
    properties:
      mail:
        smtp:
          ssl:
            trust: "*"
          auth: true
          starttls:
            enable: true

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://${infomail.dbHost}/infomail #5454
    username: root #infomail
    password: root #infomail

  #  mail:
  #    host: 'localhost'
  #    port: 1025
  #    username: infomail
  #    password: infomail
  #    properties:
  #      mail:
  #        smtp:
  #          ssl:
  #            trust: "*"
  #          auth: true
  #          starttls:
  #            enable: true
  #          connectiontimeout: 5000
  #          timeout: 3000
  #          writetimeout: 5000

springdoc:
  api-docs:
    path: '/api/v1/api-docs'

application:
  email:
    sentFrom: ${infomail.email}
    confirmation:
      tokenLink: ${infomail.reg-link}confirm?token=
      subject: InfoMail - email confirmation
    rejection:
      tokenLink: ${infomail.reg-link}reject?token=
  security:
    secret: ${infomail.secret}
